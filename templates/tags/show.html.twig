{% extends 'baseback.html.twig' %}

{% block title %}Tags{% endblock %}

{% block content %}
    <h1>Tags</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tag.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tag.description }}</td>
            </tr>
             <tr>
                <th>edit</th>
                <td>{{ tag.description }}</td>
            </tr>
            

        </tbody>
    </table>

    <a href="{{ path('app_tags_index') }}">back to list</a>

    

    {{ include('tags/delete.html.twig') }}
{% endblock %}
